node_modules/
dist/
*.log

# Local scratch: a copy of real compose files used as test fixtures is committed
# under test/fixtures/, but never a real .env.
.env

# Scratch: UI dev sandbox (database snapshot + throwaway checkout) and screenshots.
.dev/
.shots/

# Built by `npm run css` from src/styles/app.css.
public/app.css
